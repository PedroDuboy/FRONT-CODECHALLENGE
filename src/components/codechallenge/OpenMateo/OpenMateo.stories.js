import React from 'react';

import OpenMateo from '.';

export default {
    title: 'Molecules/OpenMateo'
}

export const Default = () => {
    return <OpenMateo />
}

export const Mobile = () => {
    return <OpenMateo />
}
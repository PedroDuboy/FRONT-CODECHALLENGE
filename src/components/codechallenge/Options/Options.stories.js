import React from 'react';

import Options from '.';

export default {
    title: 'Molecules/Options'
}

export const Default = () => {
    return <Options />
}

export const Mobile = () => {
    return <Options />
}